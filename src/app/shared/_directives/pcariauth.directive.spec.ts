import { PcariauthDirective } from './pcariauth.directive';

describe('PcariauthDirective', () => {
  it('should create an instance', () => {
    const directive = new PcariauthDirective();
    expect(directive).toBeTruthy();
  });
});
